<section id="products" class="container">
  <h1>All Products</h1>

  {{#each products}}
  <div class="product">
    <h3>{{name}}</h3>
    <p>{{minL}}</p>
  </div>
  {{else}}
  <p>No products available</p>
  {{/each}}

</section>